#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(void) {
    double x;
    cin >> x;
    cout << round(x) << endl;
    return 0;
}